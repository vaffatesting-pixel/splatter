{
  "name": "collider-builder",
  "private": true,
  "version": "0.0.0",
  "description": "Browser-based Gaussian splat collider authoring tool for drawing and exporting simple collision volumes.",
  "type": "module",
  "scripts": {
    "dev": "vite --port 4880 --strictPort",
    "typecheck": "tsc --noEmit",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/three": "^0.180.0",
    "typescript": "~6.0.2",
    "vite": "^8.0.4"
  },
  "dependencies": {
    "@dimforge/rapier3d-compat": "^0.19.3",
    "@sparkjsdev/spark": "^2.0.0",
    "three": "^0.180.0",
    "trystero": "^0.25.3"
  }
}
